# Supply-chain hardening (see npm worm, Aug 2026):
# never run dependency lifecycle scripts (pre/postinstall) — the worm's spread vector.
ignore-scripts=true
# always pin exact versions when adding deps
save-exact=true
